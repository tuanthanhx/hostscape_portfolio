# QA Notes

## Scope
- Implemented a standalone landing page inside `samples/wellness-experience/` only.

## Self-check
- One clear `h1` is present and all user-facing copy is Japanese.
- Required sections are included: hero, philosophy, programs, environment, benefits, guest voice, final CTA, footer.
- Palette follows `COLOR_SCHEMA.md`, with accent color reserved for primary booking actions and highlights.
- Layout is responsive with mobile and desktop breakpoints and no intentional horizontal overflow.
- Local replaceable visuals are stored in `img/` as SVG files with stable filenames.
- Soft reveal motion is progressive and disabled for users who prefer reduced motion.

## Browser QA
- No browser-based QA was run in this pass.

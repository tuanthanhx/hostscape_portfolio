# QA Notes

## Scope
- Verified all created files remain inside `samples/dining-reservation/`.
- No external assets, frameworks, or third-party scripts were added.

## Self-Check
- Confirmed each page uses `lang="ja"` and has a single `h1`.
- Confirmed shared navigation links point to local sample pages.
- Confirmed no placeholder text such as `Lorem ipsum`, `Coming soon`, or `TODO` appears in implementation files.
- Confirmed reduced-motion handling is included in `css/styles.css`.

## Remaining Verification
- Browser-based visual QA was not run in this turn.
- Recommend final review in a browser at mobile and desktop widths before marking complete.
